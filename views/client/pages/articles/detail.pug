extends ../../layout/default.pug
include ../../mixins/box-head.pug

block main 
  .article-detail 
    .container.my-3 
      .row.d-flex.justify-content-center
        .col-10.text-center
          h3(class="inner-title") #{article.title}
          .inner-content !{article.content}